# -*-coding:utf-8 -*
import os
import utils_path
import log
import common
import get_config
import web_common
import prepare_clean

#######################################################
# 函数功能：界面自动化-----多用例循环跑
# 脚本作者：duyuli
# 日期：2018-12-27
#######################################################

FILE_NAME = os.path.splitext(os.path.basename(__file__))[0]                  # 本脚本名字
script_path = "/home/StorTest/test_cases/cases/test_case/P300/WEB"
script_file_list = [
                    "web_testlink_cluster_together1.py",  # 部署相关集合
                    "web_testlink_cluster_together2.py",  # 扩容、修改、删除相关
                    "web_testlink_nas_together1.py",      # nas相关
                    "web_testlink_check_together1.py",    # 检查界面信息
                    "web_testlink_cluster_together3.py"   # 卸载集群
                   ]

def case():
    count = 0
    while True:
        count += 1
        log.info("##################### run for %s times #####################" % count)
        for script_file in script_file_list:
            log.info("start %s" % script_file)
            cmd = "python %s" % script_path + "/%s" % script_file
            rc, stdout = common.run_command_shot_time(cmd)
            if rc != 0:
                log.info("case %s failed" % script_file)
                raise Exception("unexpected result")
        log.info("############################################################")

def main():
    prepare_clean.test_prepare(FILE_NAME, env_check=False)
    case()
    prepare_clean.test_clean()
    log.info("%s succeed" % FILE_NAME)


if __name__ == '__main__':
    common.case_main(main)
