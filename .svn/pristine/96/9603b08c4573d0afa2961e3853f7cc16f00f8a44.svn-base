# -*- coding:utf-8 _*-
# Author:wangxiang
# Date  :2018-8-8
"""
测试内容:逻辑卷创建后扩容，修改容量值为整数

步骤:
1）在访问区配置中，每个节点取1块磁盘创建副本模式存储池，创建存储池pool1；
2）创建逻辑卷属性默认，选择pool1存储池，创建逻辑卷LUN1，设置容量为10GB；
3）提交后创建成功。
4）修改逻辑卷属性，将容量修改为100GB，提交。完成修改

检查项:
1）存储池创建成功
2）逻辑卷创建成功
3）修改成功
"""
import os, sys
import time
import utils_path
import Lun_managerTest
import common
import log
import get_config
import login

import decorator_func
from get_config import config_parser as cp
import env_manage_lun_manage

"""初始化日志和变量"""
FILE_NAME = os.path.splitext(os.path.basename(__file__))[0]
file_name = os.path.basename(__file__)
file_name = file_name[:-3]  # 获取本脚本名，去掉.py后缀
log_file_path = log.get_log_path(file_name)  # 获取日志目录，即test_cases/Log/Case_log
log.init(log_file_path, True)  # 初始化日志文件

node_ip1 = env_manage_lun_manage.node_ip1
node_ip2 = env_manage_lun_manage.deploy_ips[1]
client_ip1 = env_manage_lun_manage.client_ips[0]
esxi_ip = env_manage_lun_manage.Esxi_ips


def case():
    log.info("建逻辑卷属性默认，选择pool1存储池，创建逻辑卷LUN1")
    lun_id = env_manage_lun_manage.osan.create_lun(s_ip=node_ip1, total_bytes='1024000000', lun_type="THICK",
                                                   lun_name='LUN1',
                                                   stor_pool_id=2, acc_zone_id=1)
    log.info(lun_id)
    log.info('修改逻辑卷属性，将容量修改为100GB')
    env_manage_lun_manage.osan.update_lun()
    decorator_func.judge_target(
        env_manage_lun_manage.get_option_single(s_ip=node_ip1, command="get_luns", indexname="luns"
                                                , argv2="name", ids="ids", argv1=lun_id),
        'ch_lun1')


def main():
    prepare_clean.test_prepare(FILE_NAME)  # 环境准备
    case()  # 用例步骤
    env_manage_lun_manage.clean()  # 环境清理


if __name__ == '__main__':
    main()