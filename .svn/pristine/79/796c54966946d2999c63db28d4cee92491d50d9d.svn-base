关于prepare_all.py的说明如下：
1. 外层只需调用auto_install_and_prepare()函数即可完成，该函数说明如下所示：
    1.1 函数完成三部分操作：
        1.1 执行P300_AutoInstall
            1) 卸载环境：卸载集群和私有客户端
            2) 重新部署环境：重装集群和私有客户端
        1.2 执行prepare_environment()子函数，进行
            1) 创建访问区（启动nas/s3服务）
            2) 添加业务子网和对应的ip池（共两套，一套nas用，另一套s3用）
            3) 授权nfs客户端并进行挂载
        1.3 将StorTest节点作为nfs客户端，进行授权并挂载
    1.2 函数返回值：全部执行成功返回0，如有异常则退出
    1.3 函数需要的参数：
        :param log_path: Required:True   Type:string  Help:The log path. e.g. /home/StorTest/test_cases/log/case_log
        :param svip_num: Required:False   Type:int  Help:The number of svips. e.g. 1 or 2

2. 需要在配置文件unistor_test_config.xml、p300_nas_config.xml配置文件中写入相关内容
    2.1 unistor_test_config.xml需要写入的内容如下：
        <nodes>
            <node>
                <ctrl_ip>10.2.42.65</ctrl_ip>  # 任意集群节点的ip
            </node>
        </nodes>

        <clients>
            <client>
                <ctrl_ip>20.2.42.80</ctrl_ip>  # 私有客户端ip，需要与数据网同网段
            </client>
        </clients>

        <volumes>
            <volume>parastor_nas</volume>    # nas客户端使用的卷名称
            <volume>parastor_posix</volume>  # posix客户端使用的卷名称
        </volumes>

        <client_install_path>/root/client_setup/client_20190123_195614</client_install_path> # 集群节点和客户端节点安装包存放位置，需要是空目录


    2.2 p300_nas_config.xml需要写入的内容如下：
        <subnet_info>
            <ip_families>
                <ip_family>IPv4</ip_family>   # svip的类型
            </ip_families>
            <svips>
                <svip>70.2.42.231</svip>   # s3用
            </svips>
            <svips>
                <svip>60.2.42.231</svip>   # nas用
            </svips>

            <subnet_masks>
                <subnet_mask>22</subnet_mask>  # svip绑定到的网卡的子网掩码
            </subnet_masks>

            <network_interfaces>
                <network_interface>ens256</network_interface>         # 一张网卡名，s3用
            </network_interfaces>
            <network_interfaces>
                <network_interface>ens224</network_interface>         # 一张网卡名，nas用
            </network_interfaces>
        </subnet_info>

        <vip_address_pool_info>
            <domain_names>
                <domain_name>www.sugons3.com</domain_name>  # s3用ip池的域名
            </domain_names>
            <domain_names>
                <domain_name>www.sugonnas.com</domain_name>  # nas用ip池的域名
            </domain_names>
            <vip_addresses>
                <vip_addresse>60.2.42.232,60.2.42.233,60.2.42.234,60.2.42.235</vip_addresse> # 离散型vip池地址，nas用
            </vip_addresses>
            <vip_addresses>
                <vip_addresse>70.2.42.232,70.2.42.233,70.2.42.234,70.2.42.235</vip_addresse> # 离散型vip池地址，s3用
            </vip_addresses>
        </vip_address_pool_info>

        <nfs_client_info>
            <nfs_client_ip>
                <nfs_client_ip>10.2.40.34</nfs_client_ip>   # nfs客户端ip地址
                <nfs_client_ip>10.2.40.95</nfs_client_ip>   # StorTest节点(挂载nfs，存放s3数据一致性校验的源文件)用
            </nfs_client_ip>
        </nfs_client_info>

        <access_zone_info>
            <access_zone_names>
                    <access_zone_name>sugonnas</access_zone_name>  # 访问区的名称
            </access_zone_names>
        </access_zone_info>

3. 其他注意事项：
    3.1 系统安装包需要放到本节点(StorTest执行部署环境操作的节点)的路径：/home/StorTest/src_code/P300下，
        安装包名称如：ParaStor-3.0.0-centos7.5-feature_ofs3.0_lastdebug_30b126b_fea3e71_20190219_134050-2-1.tar
    3.2 需要将安装环境所需的deploy_config.xml配置文件放到本节点(StorTest执行部署环境操作的节点)的路径：/home/StorTest/conf下，并
        命名为deploy_config_p300.xml
    3.3 StorTest节点需要和nas/s3的业务网均能ping通
