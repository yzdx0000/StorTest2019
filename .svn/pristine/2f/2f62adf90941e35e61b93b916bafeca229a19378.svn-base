#!/usr/bin/python2.6
# -*- coding: utf-8 -*-

#######本脚本用于列出bucket对象###########
#需要执行list_objects_exc，参数包括：oOss节点ip，bucket名称和certificateid
#函数返回一个元组(return_code,output)。第一个元素为0表示成功，为256表示失败；第二个元素返回命令执行打印信息

import subprocess,commands

#添加自定义模块*****
import _create_logfile_zh    #记录日志模块
import read_conf_zh    #获取conf中变量的模块

def list_objects_cmd(ip,bucket_name,certificate_id):
    cmd='curl -i -s http://'+str(ip)+':20480/'+str(bucket_name)+' -X GET -H '+' \"Authorization: AWS4-HMAC-SHA256 Credential='+str(certificate_id)+'\"'  #执行查询所有者bucket列表命令本身
    return cmd

def list_objects_exc(ip,bucket_name,certificate_id):
    cmd=list_objects_cmd(ip,bucket_name,certificate_id)
    print cmd
    (return_code,output)=commands.getstatusoutput(cmd)
    print return_code
    print output
    return (cmd,return_code,output)
#函数自验*****
if __name__=="__main__":
    all_argvs=read_conf_zh.get_argvs()
#    list_objects_exc(ds_ip,bucket_name,certificate_id)
    list_objects_exc(all_argvs['oossip'],all_argvs['create_bucket_name'],all_argvs['certificate_id'])   

