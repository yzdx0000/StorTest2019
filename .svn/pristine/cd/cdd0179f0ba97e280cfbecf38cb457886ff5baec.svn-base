lun_manager模块为卷管理用例，执行本模块用例需满足条件如下:
 1、集群节点，目前只能为3节点(当时写死了)
 2、存储池，至少为3个
 3、lun条带应该是3
 4、每个节点至少需要3块共享盘，不然某些用例会有问题！

关于卷管理的用例，直接执行当前目录的环境部署的脚本,env_manage_lun_manager.py，可确保测试环境满足条件

注意:在case_list里，需要enable_san后操作的用例，如主机组、主机、initiator类用例应该在存储池、节点池、访问区类用例前面执行