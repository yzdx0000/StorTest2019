#!/usr/bin/python2.6
# -*- coding: utf-8 -*-

#######本脚本用于删除账户认证###########
#需要执行del_certificate_exc，参数包括：oOms节点ip，certificateid
#函数返回一个元组(return_code,output)。第一个元素为0表示成功，为256表示失败；第二个元素返回命令执行打印信息

import subprocess,commands    #调用子进程执行shell命令

#添加自定义模块*****
import _create_logfile_zh    #记录日志模块
import read_conf_zh    #获取conf中变量的模块

#定义获取删除证书命令函数，第一个参数是mgr节点的ip；第二个参数是要删除的证书id，以字符串的形式返回删除证书的命令
def del_certificate_cmd(ip,certificate_id):
    cmd='/home/parastor/bin/sysctl/parastor_pos'+' delcertificate'+' certificateid='+str(certificate_id)  #执行删除账户认证命令本身
    cmd='ssh '+str(ip)+' -C '+"'"+cmd+"'"   #到管理节点去执行删除账户认证命令
    return cmd

#定义删除证书函数，第一个参数是mgr节点的ip；第二个参数是>要删除的证书id
def del_certificate_exc(ip,certificate_id):
    cmd=del_certificate_cmd(ip,certificate_id) #获取删除证书的命令
    print cmd
    (return_code,output)=commands.getstatusoutput(cmd)
    print return_code
    print output
    return (cmd,return_code,output)
#函数自验*****
if __name__=="__main__":
    all_argvs=read_conf_zh.get_argvs()
#    del_certificate_exc(all_argvs['master_mgr_ip'],all_argvs['certificate_id'])
    del_certificate_exc(all_argvs['master_mgr_ip'],'00003P6T9RXMXKRA00003P6T9RK8BY34')
